# FIDO2 Client Simulator Configuration
# This file contains configuration for the FIDO2 Client Simulator

# Paths for credential storage
# You can use relative or absolute paths
keystore.path=fido2_keystore.p12
metadata.path=fido2_metadata.json

# Security settings
# Note: For production use, this should be changed and stored securely
keystore.password=fido2simulator

# Logging configuration
log.level=INFO
